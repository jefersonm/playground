Global setup:
 Set up git
  git config --global user.name "Jeferson Machado"
  git config --global user.email jefersonm@gmail.com
      
Next steps:
  mkdir sandbox
  cd sandbox
  git init
  touch README
  git add README
  git commit -m 'first commit'
  git remote add origin git@github.com:jefersonm/sandbox.git
  git push -u origin master
      
Existing Git Repo?
  cd existing_git_repo
  git remote add origin git@github.com:jefersonm/sandbox.git
  git push -u origin master
      
Importing a Subversion Repo?
  Click here
      
When you're done:
  Continue