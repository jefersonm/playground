package com.github.jefersonm.sandbox.scala.learnscala

object BasicExamples {

	def main(args: Array[String]){
		println("Hello World!")
		
		val x = 2
		
		println(1 + 2 * 3 / x) //Numbers are objects as you can see at the line bellow
		println( (1).+(((2).*(3))./(x)) )
	}

}